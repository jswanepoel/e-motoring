export interface ICredential {
  username: string;
  detail: string;
  userNameDTL: string;
  detailDTL: string;
  baseActionValue: string;
  baseFunctionValue: string;
  baseRequiredDatabase: any[]; 
}
