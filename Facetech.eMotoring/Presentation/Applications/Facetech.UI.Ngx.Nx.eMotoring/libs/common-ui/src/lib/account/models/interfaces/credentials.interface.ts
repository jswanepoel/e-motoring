export interface Credentials {
  username: string;
  detail: string;
  userNameDTL: string;
  detailDTL: string;
  baseActionValue: string;
  baseFunctionValue: string;
  baseRequiredDatabase: any[]; 
}
